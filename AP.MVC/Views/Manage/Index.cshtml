@model AP.MVC.Models.IndexViewModel
@{
    ViewBag.Title = "Mi cuenta";
}

<main aria-labelledby="title">
    <h2 id="title">@ViewBag.Title</h2>

    <p class="text-success">@ViewBag.StatusMessage</p>
    <div>
        <h4>Cambie la configuración de su cuenta</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>Contraseña:</dt>
            <dd>
                @if (Model.HasPassword)
                {
                    @Html.ActionLink("Cambiar su contraseña", "ChangePassword")
                }
                else
                {
                    @Html.ActionLink("Crear una contraseña", "SetPassword")
                }
            </dd>
        </dl>
    </div>
</main>
