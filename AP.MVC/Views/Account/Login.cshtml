@using AP.MVC.Models
@model LoginViewModel
@{
    ViewBag.Title = "Iniciar sesión";
}

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Baloo+2:wght@600;700;800&family=Nunito+Sans:wght@400;600;700&family=JetBrains+Mono:wght@500;700&display=swap" rel="stylesheet">
<link href="~/Content/auth.css" rel="stylesheet">

<main aria-labelledby="title" class="auth-stage">
    <div class="auth-side">
        <div class="auth-op op1">&#247;</div>
        <div class="auth-op op2">&times;</div>
        <div class="auth-op op3">+</div>
        <p class="auth-wordmark">Mathema<span>X</span></p>
        <p class="auth-tagline">Entrená la mente, subí de nivel y volvete el mejor jugador de números del salón.</p>
        <div class="auth-modes">
            <div class="auth-mode mode-op">
                <div class="auth-mode-badge">?=</div>
                <div>
                    <div class="auth-mode-name">Operador perdido</div>
                    <div class="auth-mode-desc">Encontrá el signo que falta</div>
                </div>
            </div>
            <div class="auth-mode mode-time">
                <div class="auth-mode-badge">00:0</div>
                <div>
                    <div class="auth-mode-name">Contrarreloj</div>
                    <div class="auth-mode-desc">Resolvé antes de que se acabe el tiempo</div>
                </div>
            </div>
            <div class="auth-mode mode-xp">
                <div class="auth-mode-badge">&darr;9</div>
                <div>
                    <div class="auth-mode-name">Secuencias lógicas</div>
                    <div class="auth-mode-desc">Encontrá el patrón oculto</div>
                </div>
            </div>
        </div>
    </div>

    <div class="auth-panel">
        <section id="loginForm">
            @using (Html.BeginForm("Login", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { role = "form" }))
            {
                @Html.AntiForgeryToken()
                <h2 id="title">@ViewBag.Title</h2>
                <p class="auth-sub">Use una cuenta local para iniciar sesión.</p>

                @Html.ValidationSummary(true, "", new { @class = "auth-error" })

                <div class="auth-field">
                    @Html.LabelFor(m => m.Email)
                    @Html.TextBoxFor(m => m.Email, new { @class = "form-control", placeholder = "nombre@correo.com" })
                    @Html.ValidationMessageFor(m => m.Email, "", new { @class = "text-danger" })
                </div>

                <div class="auth-field">
                    @Html.LabelFor(m => m.Password)
                    @Html.PasswordFor(m => m.Password, new { @class = "form-control", placeholder = "••••••••" })
                    @Html.ValidationMessageFor(m => m.Password, "", new { @class = "text-danger" })
                </div>

                <div class="auth-row">
                    <label class="checkbox-inline">
                        @Html.CheckBoxFor(m => m.RememberMe)
                        @Html.LabelFor(m => m.RememberMe)
                    </label>
                    @* Habilitar cuando la confirmación de cuenta esté activa para restablecer contraseñas
                        @Html.ActionLink("¿Olvidó su contraseña?", "ForgotPassword")
                    *@
                </div>

                <input type="submit" value="Iniciar sesión" class="auth-btn" />

                <p class="auth-footer-link">
                    ¿Todavía no tenés cuenta? @Html.ActionLink("Registrarse como nuevo usuario", "Register")
                </p>
            }
        </section>

        <section id="socialLoginForm" style="margin-top:20px;">
            @Html.Partial("_ExternalLoginsListPartial", new ExternalLoginListViewModel { ReturnUrl = ViewBag.ReturnUrl })
        </section>
    </div>
</main>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
